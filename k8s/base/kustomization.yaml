apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base compartilhada aplicada a todos os módulos (api, web, postgres).
resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - ingress.yaml
  - postgres/deployment.yaml
  - postgres/service.yaml