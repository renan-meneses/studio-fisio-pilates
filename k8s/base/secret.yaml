apiVersion: v1
kind: Secret
metadata:
  name: clinica-secrets
  namespace: clinica
type: Opaque
stringData:
  # Placeholders — troque por valores reais (ou um secret externo/Vault) em produção.
  POSTGRES_PASSWORD: "trocar-em-producao"
  JWT__KEY: "trocar-por-chave-aleatoria-de-no-minimo-32-bytes"